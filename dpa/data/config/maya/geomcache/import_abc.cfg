
global:
    
    options:

        merge_under_selection:

            type: bool
            default: True
            help: Merge the alembic cache under the current selection.
            label: "Merge under selection"

