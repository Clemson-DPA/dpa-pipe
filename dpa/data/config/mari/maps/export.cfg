global:
    
    options:

        tif_export:

            type: bool
            default: True
            help: Export the entity as .tif files.
            label: "Export maps to TIF"
            disabled: True

        tex_export:

            type: bool
            default: True
            help: Also convert .tif to .tex files.
            label: "Convert TIF to TEX"